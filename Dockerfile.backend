FROM node:22-alpine AS base
WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY prisma ./prisma
COPY server ./server

ENV NODE_ENV=production
EXPOSE 8000

CMD ["sh", "-c", "npm run prisma:migrate:deploy && node server/app.js"]
